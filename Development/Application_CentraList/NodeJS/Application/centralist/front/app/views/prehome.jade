div.container
  div.row
    div.col-sm-offset-2.col-sm-8.col-md-offset-3.col-md-6.listing-shake
      form.well.listing-panels.form-horizontal(ng-submit="submit()")
        h2 Connexion à CentraList
        hr

        alert(ng-repeat="alert in alerts", type="alert.type", ng-animate="'listing-alert'") {{ alert.msg }}
        div.form-group
          label.col-sm-4.control-label(for="login") Login
          div.col-sm-8
            input.form-control(type="text", id="login", placeholder="Login", ng-model="login")
        div.form-group
          label.col-sm-4.control-label(for="pass") Mot de passe
          div.col-sm-8
            input.form-control(type="password", id="pass", placeholder="Mot de passe", ng-model="pass")
        div.form-group
          div.col-sm-offset-4.col-sm-8
            button.btn.btn-primary.btn-lg(type="submit") Connexion
            = ' '
            //button.btn.btn-default.btn-lg(type="button", ng-click="cas()") CAS Ec-lille

      accordion(close-others="true")
        accordion-group
          accordion-heading Je n'ai pas encore de compte
          | Il te suffit d'envoyer un mail à l'adresse 
          a(href="mailto:services.eleves.eclille@gmail.com") services.eleves.eclille@gmail.com
          |  pour en faire la demande
        accordion-group
          accordion-heading J'ai oublié mon mot de passe
          | Tu peux demander un nouveau mot de passe via le service 
          a(href="https://passwordreset.ec-lille.fr/") PasswordReset

